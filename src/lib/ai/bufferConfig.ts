/**
 * Buffer Inteligente window — hardcoded for now (docs/blueprint/04-inbox.md
 * says "configurable por workspace", but no field models that anywhere in
 * the schema yet). Scope cut, flagged, not silent.
 */
export const DEFAULT_BUFFER_WINDOW_SECONDS = 10;
